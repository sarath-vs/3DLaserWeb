import 'package:flutter/material.dart';

class QulityMannual extends StatelessWidget {
  const QulityMannual({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
